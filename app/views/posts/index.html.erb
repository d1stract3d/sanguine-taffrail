<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-sm">
    <h1>Posts</h1>
  </div>
  <div class="col-8"></div>
  <div class="col-sm">
    <a href="/" class="btn btn-danger btn-lg active" role="button" aria-pressed="true">< Go Home</a>
  </div>
</div>

<div class="row pl-3">
<table>
  <thead>
  <tr>
    <th>of</th>
    <th>text</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.body %></td>
      <td><%= link_to 'Show', post %></td>
      <td><%= link_to 'Edit', edit_post_path(post) %></td>
      <td><%= link_to 'Destroy', post, method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
<br>

<%= link_to 'New Post', new_post_path %>
